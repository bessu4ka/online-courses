import { Pagination } from 'components/Pagination';

import styled from './BlogPage.module.scss';

const BlogPage = () => {
  return (
    <section>
      <Pagination />
    </section>
  );
};

export { BlogPage };
